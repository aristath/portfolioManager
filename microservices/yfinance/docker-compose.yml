version: '3.8'

services:
  yfinance:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yfinance-service
    ports:
      - "9003:9003"
    environment:
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9003/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
