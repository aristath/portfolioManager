profiles:
  default:
    fqbn: arduino:zephyr:unoq
    platforms:
      - platform: arduino:zephyr
    libraries:
      - ArduinoGraphics
      # RouterBridge is included with arduino:zephyr platform, no need to install separately
default_profile: default
